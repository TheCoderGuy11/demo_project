<p class="h1">Category/<%= @category.name %></p><hr/>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <% @sub_categories.each do |sc| %>
    <tr>
      <td><%= sc.name %></td>
      <td><%= link_to 'Show', sub_category_category_path(sc.id), class: "btn btn-info" if is_customer? %></td>
    </tr>
  <% end %>
</table>
