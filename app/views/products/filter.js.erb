<% if @item.present? %>
  $("#variant").html("<%= escape_javascript options_for_select(Variant.all.map { |u|  [u.name, u.id] }) %>");
<% elsif @variant.present? %>
  $("#value").html("<%= escape_javascript options_for_select(VariantValue.all.map { |u| [u.name, u.id] }) %>");
<% end %>